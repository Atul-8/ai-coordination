# META 规则 — 项目级蒸馏池

> G3 五步法的产物。一条规则 = 一次踩坑换取的永久复利。
> 新规则编号前先 `git pull`（若 .ai 独立成库）并查看最大编号，避免冲突。
> 全局池（默认 `C:\.ai_global`，`AI_GLOBAL_DIR` 可覆盖）：确认普适后由 /meta 提升，全局编号 META-NNNN。

## 受控词表（类别.主题）

| 类别 | 取值 |
| --- | --- |
| 前缀 | 构建 BUILD / 环境 ENV / 测试 TEST / 数据 DATA / 接口 API / 架构 ARCH / 流程 PROC / 文档 DOC / 安全 SEC / 性能 PERF / 依赖 DEP / 协作 COLLAB |

## 规则列表

### META-000 [PROC.流程] 示例规则（编号连续、不重号）

- **规则**: 一句话可执行的规则。
- **来源**: ERR-000 / 会话 01a05cc8
- **反面案例**: （什么做法会再踩坑）
